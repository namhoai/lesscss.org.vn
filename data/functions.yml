# =============================================
# PUBLISHED
# =============================================
# This file is only needed to ensure that the
# sections build in a specific order.


# The base path for features docs. Makes this easier
# to update without having to change each path.
base: content/functions

published:
  misc-functions:
    title: Các function hỗn hợp
    files: <%= functions.base %>/misc-functions.md

  string-functions:
    title: Các function xử lý chuỗi
    files: <%= functions.base %>/string-functions.md

  list-functions:
    title: Các function xử lý dữ liệu danh sách
    files: <%= functions.base %>/list-functions.md

  math-functions:
    title: Các function tính toán
    files: <%= functions.base %>/math-functions.md

  type-functions:
    title: Các function liên quan đến kiểu dữ liệu
    files: <%= functions.base %>/type-functions.md

  color-definition:
    title: Các function định nghĩa màu sắc
    files: <%= functions.base %>/color-definition.md

  color-channel:
    title: Các function xử lý kênh màu sắc
    files: <%= functions.base %>/color-channel.md

  color-operations:
    title: Các function tính toán màu sắc
    files: <%= functions.base %>/color-operations.md

  color-blending:
    title: Các function hòa trộn màu sắc
    files: <%= functions.base %>/color-blending.md


# =============================================
# NOT PUBLISHED / PLANNED / WIP
# =============================================

unpublished:
